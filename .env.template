# Secrets live in 1Password (vault: dev, item: personal-draft-yahoo). Never write real values here.
# Run the app with:  op run --env-file=.env.template -- .venv/bin/streamlit run app.py
YAHOO_CLIENT_ID=op://dev/personal-draft-yahoo/client_id
YAHOO_CLIENT_SECRET=op://dev/personal-draft-yahoo/client_secret

# Optional: skip the league picker by naming the league key directly, e.g. 461.l.123456
YAHOO_LEAGUE_KEY=
